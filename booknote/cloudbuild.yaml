steps:
  # Step 1: Build the Docker image for the Spring Boot app
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/booknote', '.']

  # Step 2: Push the Docker image to Google Container Registry (GCR)
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/booknote']

  # Step 3: Deploy the Docker image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args: ['run', 'deploy', 'booknote', '--image', 'gcr.io/$PROJECT_ID/booknote', '--platform', 'managed', '--region', 'us-central1', '--allow-unauthenticated']

# Optional: Specify substitutions (if needed)
substitutions:
  _SERVICE_NAME: 'booknote'

# Optional: Define where the artifacts will be stored (if needed)
artifacts:
  images:
    - 'gcr.io/$PROJECT_ID/booknote'
